version: "3.3"

services:
  backend:
    build: .
    restart: always
    working_dir: /app
    volumes:
      - ./:/app
    container_name: frontend_app
    ports:
      - 3000:3000
    stdin_open: true

# docker-compose up -d
